# ERPNext stack env — copy to `.env` (chmod 600) next to docker-compose.yml.
# Real values live in the vault (file.vps-secrets), NOT in git.
SITE_NAME=erp.swe.com.ly
SITE_DB_NAME=erp_swe
DB_PASSWORD=__CHANGE_ME__
ADMIN_PASSWORD=__CHANGE_ME__
